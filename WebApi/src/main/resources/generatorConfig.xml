<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN" "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
    <!--引入配置文件-->
    <properties resource="dbConfig.properties"></properties>
    <!-- 数据库驱动包位置 -->
<classPathEntry location="${driverClassPath}"/>
  <context id="mysqlTables" targetRuntime="MyBatis3">
    <commentGenerator>
    <!-- 关闭注释
         suppressAllComments  false时打开注释，true时关闭注释
		 suppressDate  false时打开时间标志，true时关闭. -->
    	<property name="suppressDate" value="false"/>
    	<property name="suppressAllComments" value="false"/>
    </commentGenerator>
    <!-- 数据库链接URL、用户名密码配置 -->
    <jdbcConnection connectionURL="${jdbc.jdbcUrl}" driverClass="${jdbc.driverClass}"  password="${jdbc.password}" userId="${jdbc.user}" />

    <!-- mybatis里专门用来处理NUMERIC和DECIMAL类型的策略 -->
      <!-- 非必需，类型处理器，在数据库类型和java类型之间的转换控制-->
    <javaTypeResolver>
    	<property name="forceBigDecimals" value="false"/>
    </javaTypeResolver>
    
    <!-- 实体类存放位置 -->
    <javaModelGenerator targetPackage="${model.package}" targetProject="${target.project}">
        <!-- 是否允许子包，即targetPackage.schemaName.tableName -->
    	 <property name="enableSubPackages" value="true" />
        <!-- 是否对model添加 构造函数 -->
        <property name="constructorBased" value="true"/>
        <!-- 是否对类CHAR类型的列的数据进行trim操作 -->
         <property name="trimStrings" value="false" />
        <!-- 建立的Model对象是否 不可改变  即生成的Model对象不会有 setter方法，只有构造方法 -->
        <property name="immutable" value="false"/>
    </javaModelGenerator>
    
    <!-- 映射文件存放位置 -->
    <sqlMapGenerator targetPackage="${xml.mapper.package}"  targetProject="${target.project}">
	    <property name="enableSubPackages" value="false" />
    </sqlMapGenerator> 
    <!-- Dao的存放位置 -->
    <javaClientGenerator targetPackage="${dao.package}"  targetProject="${target.project}" type="XMLMAPPER">
    	<property name="enableSubPackages" value="false"/>
    </javaClientGenerator>

	<!-- 要生成的那些表(更改tableName 和domainObjectName 就可以了) --> 
    <table schema="helpyou" tableName="User" domainObjectName="User"
           enableCountByExample="false"
           enableDeleteByExample="false" enableSelectByExample="false" enableSelectByPrimaryKey="false"
           enableDeleteByPrimaryKey="false" enableUpdateByPrimaryKey="false" enableUpdateByExample="false"
           enableInsert="false">
      <property name="useActualColumnNames" value="true"/>

    </table>
  </context>
</generatorConfiguration>